# Admin credentials for the workshop login popup
# These are exposed to the browser after Vite build — acceptable for a temporary workshop
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=workshop2026

# Your deployed backend URL (update after deploying to Render)
VITE_API_URL=http://localhost:3001
